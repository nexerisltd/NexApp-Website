diff --git a/src/components/AnimatedGrid.tsx b/src/components/AnimatedGrid.tsx
index 2ce7b47..2d000da 100644
--- a/src/components/AnimatedGrid.tsx
+++ b/src/components/AnimatedGrid.tsx
@@ -26,7 +26,7 @@ export default function AnimatedGrid({
   return (
     <motion.div variants={container} initial="hidden" animate="show" className={className}>
       {children.map((child, i) => (
-        <motion.div key={i} variants={item}>
+        <motion.div key={i} variants={item} className="h-full">
           {child}
         </motion.div>
       ))}
diff --git a/src/components/AppCard.tsx b/src/components/AppCard.tsx
index 0f21a8c..a03b7d3 100644
--- a/src/components/AppCard.tsx
+++ b/src/components/AppCard.tsx
@@ -12,7 +12,7 @@ export default function AppCard({ app }: { app: App }) {
     <motion.div
       whileHover={{ y: -4 }}
       transition={{ type: "spring", stiffness: 300, damping: 22 }}
-      className="relative"
+      className="relative h-full"
     >
       <div className="absolute right-3 top-3 z-10">
         <FavoriteButton appId={app.id} variant="icon" />
