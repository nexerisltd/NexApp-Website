diff --git a/src/components/ShopBrowser.tsx b/src/components/ShopBrowser.tsx
index 5aeb018..f02e5a9 100644
--- a/src/components/ShopBrowser.tsx
+++ b/src/components/ShopBrowser.tsx
@@ -23,8 +23,8 @@ export default function ShopBrowser({
   const router = useRouter();
   const searchParams = useSearchParams();
 
-  const [query, setQuery] = useState(searchParams.get("q") ?? "");
-  const [category, setCategory] = useState(searchParams.get("category") ?? "");
+  const [query, setQuery] = useState(() => searchParams.get("q") ?? "");
+  const [category, setCategory] = useState(() => searchParams.get("category") ?? "");
   const [apps, setApps] = useState<App[]>(initialApps);
   const [isSearching, setIsSearching] = useState(false);
   const [, startTransition] = useTransition();
@@ -61,7 +61,7 @@ export default function ShopBrowser({
   useEffect(() => {
     if (isFirstRun.current) {
       isFirstRun.current = false;
-      if (!query.trim() && !category) return;
+      if (!(query ?? "").trim() && !category) return;
     }
 
     if (debounceRef.current) clearTimeout(debounceRef.current);
@@ -76,7 +76,7 @@ export default function ShopBrowser({
         .eq("status", "published")
         .order("downloads_count", { ascending: false });
 
-      const trimmed = query.trim();
+      const trimmed = (query ?? "").trim();
       if (trimmed) {
         dbQuery = dbQuery.or(`name.ilike.%${trimmed}%,app_code.ilike.%${trimmed}%`);
       }
